var errorHandler = require('../../error/error-handling'),
  app = require('../../server');

describe('errorHandler', function () {

  it('should send back the status and the error', function (done) {
    console.log('SetUp');
    done();
  });
})
